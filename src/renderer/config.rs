/// Contains configuration options for the renderer like the resolution, vsync, and other settings
pub struct RenderConfig {

}

impl RenderConfig {
    pub fn new() -> RenderConfig {
        todo!()
    }
}

